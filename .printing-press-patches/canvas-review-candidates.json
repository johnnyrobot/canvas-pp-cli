{
  "schema_version": 2,
  "id": "canvas-review-candidates",
  "applied_at": "2026-08-07",
  "base_run_id": "20260622-151431-06e611a5",
  "base_printing_press_version": "4.25.0",
  "summary": "fix(canvas): repair `which` discovery, bound MCP responses, de-duplicate primary-key extraction, cover credential resolution, surface silent mirror failures, drop inert scaffolding",
  "reason": "An architecture review of the printed CLI surfaced six defects and duplications in generator-owned files. Three were live user-facing bugs: `which` — the discovery command CLAUDE.md, AGENTS.md and SKILL.md all point agents at first — returned wrong or empty results for every query tried; the MCP server applied no size ceiling to tool results, returning 973,781 bytes for a 4,000-item list straight into an agent's context while 280 lines of working truncation logic sat unreachable; and local-mirror write failures were discarded at 400 call sites, leaving a broken store silently serving stale data. The rest were a duplicated primary-key rule across two packages, a security-critical credential resolver with zero tests, and generator scaffolding that never applied to Canvas.",
  "files": [
    "internal/cli/which.go",
    "internal/cli/which_search_test.go",
    "internal/mcp/code_orch.go",
    "internal/mcp/tools.go",
    "internal/mcp/code_orch_bounds_test.go",
    "internal/cli/sync.go",
    "internal/store/extract_resource_id_test.go",
    "internal/config/config.go",
    "internal/config/config_test.go",
    "internal/cli/data_source.go",
    "internal/cli/data_source_mirror_test.go",
    "internal/client/client.go",
    "internal/cache/cache.go",
    "internal/types/types.go",
    "internal/cliutil/odata_date.go",
    "internal/cliutil/odata_date_test.go",
    "internal/cliutil/extractnumber.go",
    "internal/cliutil/extractnumber_test.go",
    "internal/cliutil/jwtshape.go",
    "internal/cliutil/jwtshape_test.go"
  ],
  "validated_outcome": "go test ./... green across all 7 packages; go vet and gofmt clean; golangci-lint (with a v2-compatible config — see the canvas-golangci-config-version note) shows 739 findings against main's 743, with zero introduced by these changes. 30 tests added across which resolution, MCP response bounding, primary-key extraction, credential resolution and mirror-failure reporting. Every rule mutation-checked; two mutations initially survived and the tests were rewritten until they did not. internal/config went from zero tests to 13.",
  "deferred_to_upstream": [
    {
      "feature": "Generated read commands should call resolveRead(...) rather than resolveReadWithStrategy(..., \"auto\", ...).",
      "reason": "Machine-level, and a one-line template change. All 524 generated read call sites pass the identical literal \"auto\", and resolveRead already exists as exactly that 9-parameter wrapper with zero callers. The strategy parameter is a seam with one adapter: it adds width to a 10-parameter interface without adding behaviour. This was deliberately NOT fixed locally — the local form is a 524-file diff that fixes no bug, enables no test, and is undone by the next print, while the template form fixes every printed CLI permanently. The transformation is mechanical: resolveReadWithStrategy(cmd.Context(), c, flags, \"auto\", -> resolveRead(cmd.Context(), c, flags,"
    },
    {
      "feature": "Do not emit API-shape helpers into printed CLIs whose spec cannot use them.",
      "reason": "internal/types shipped as an empty package. internal/cache shipped with zero importers, duplicating the file cache internal/client already implements inline. cliutil/odata_date.go, extractnumber.go and jwtshape.go cite Exact Online, Dynamics 365, Binance, Coinbase, Stripe and cookie-jar JWT scraping in their own doc comments — none apply to a bearer-token REST API. Emitting them costs every printed CLI dead weight and lint noise. Note the inverse mistake is also easy: cliutil/fanout.go and probe.go also had zero call sites but are tested, correct and plausibly useful here, so they were kept."
    },
    {
      "feature": "Generated helpers should be wired to the shared implementation rather than reimplemented inline.",
      "reason": "client.go recomputed the 2^attempt retry curve inline while cliutil.Backoff sat unused implementing the same curve plus a MaxBackoff ceiling. The template emitted both the helper and a copy of its body."
    },
    {
      "feature": "The which ranker template needs the fixes applied here: tokenise on non-alphanumerics, score every entry field, match by whole token, and fall back to the command tree.",
      "reason": "Machine-level. strings.Fields tokenisation makes every hyphenated command unmatchable in any printed CLI, not just this one. Substring group matching lets a two-letter query token match inside an unrelated word. WhyItMatters is written into the index by the generator and then never scored. And a curated index of hero features with no command-tree fallback makes the entire endpoint surface undiscoverable — here, 6 entries against ~1,000 commands. The tree walk must recurse into Hidden commands: every per-resource area group is emitted Hidden, so skipping hidden subtrees hides everything."
    },
    {
      "feature": "The MCP code-orchestration execute path should route its result through the response-bounding helper the template already emits.",
      "reason": "Machine-level. handleCodeOrchExecute returned the raw body while mcpToolResultText — the 60,000-byte / 50-item ceiling with preview envelopes — was reachable only from makeAPIHandler, the superseded per-endpoint registration path with zero callers. Every printed CLI with code-orchestration mode has the same unbounded path and the same four tests guarding code that cannot run."
    },
    {
      "feature": "Primary-key extraction should exist once. The generator emits it into both internal/store and internal/cli.",
      "reason": "Machine-level. 86 of 105 non-comment lines were byte-identical across the two copies, five helper names shared verbatim, and both carried the same comment warning that 'divergence here produces silent drops on heterogeneous payloads'. internal/cli's copy had a single caller and now calls store.ExtractResourceID; equivalence was verified over 250 resource/payload combinations before deleting."
    },
    {
      "feature": "writeMutationResponseToStore should not discard errors, and templates should give best-effort background work a way to report failure.",
      "reason": "Machine-level. The template emits `if err != nil { return }` on open and `_, _, _ =` on the upsert, at 400 call sites per printed CLI. Because the function takes no writer, the local fix warns once per process to os.Stderr; a template-level fix could thread a writer through instead."
    }
  ]
}
