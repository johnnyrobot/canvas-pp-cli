{
  "schema_version": 2,
  "id": "form-encode-rails-bracket-params",
  "applied_at": "2026-08-23",
  "base_run_id": "20260622-151431-06e611a5",
  "base_printing_press_version": "4.25.0",
  "summary": "Send request bodies whose parameter names use Rails bracket convention as application/x-www-form-urlencoded instead of application/json, so Canvas actually sees them. Every write command in the CLI and MCP server was silently no-opping with HTTP 200.",
  "reason": "The generated command tree builds request bodies as a flat map keyed by the parameter names straight out of the Canvas API docs, which are Rails bracket names: body[\"wiki_page[body]\"], body[\"assignment[name]\"], body[\"course_ids[]\"]. internal/client/client.go then json.Marshal'd that map and sent it as application/json. Canvas is a Rails app, and Rails only expands bracket names into a nested params hash when they arrive as a query string or a form-encoded body -- that expansion is Rack's parse_nested_query. A JSON body is handed to JSON.parse and merged into params verbatim, with no bracket expansion, so {\"wiki_page[body]\": \"x\"} lands under the literal key \"wiki_page[body]\" and params[:wiki_page] stays nil.\n\nThe failure mode was silent, which is why it went unnoticed: Canvas answered PUT /courses/87/pages/<url> with HTTP 200 and the unmodified page, so the CLI reported success and printed a page object while nothing had changed. Only endpoints with a required attribute surfaced anything at all, and then misleadingly -- `pages create` returned 400 \"Title can't be blank\" while a title was plainly being sent.\n\nBlast radius was the whole write surface: 1,042 bracketed body keys across 155 files in internal/cli, spanning 87 distinct parameter prefixes (account, assignment, course, enrollment, submission, quiz, ...), plus the same surface re-exposed as MCP tools. Fixing it at the 1,042 call sites would not survive a reprint and would fight the generator, so the encoding decision is made once at the single client chokepoint where the body is serialized.\n\nOnly top-level keys are inspected for a bracket. A body handed in through --stdin as genuine nested JSON ({\"wiki_page\":{\"body\":\"x\"}}) has no bracketed key, stays on the JSON path, and keeps working -- Canvas accepts that form too, and it was the only way to write through this CLI before the fix.\n\nUpstream: the generator should decide body encoding from the parameter-name convention of the spec it is printing, rather than defaulting every body to JSON. A Rails-style API described with bracket parameter names cannot be driven with a JSON body.",
  "files": [
    "internal/client/formencode.go",
    "internal/client/formencode_test.go",
    "internal/client/client.go"
  ],
  "known_limitations": "Three generated key shapes are malformed independently of this patch and are not addressed by it: leading-bracket names (body[\"[]account_id\"] in lti_contextcontrols_post_bulk.go and lti_resource_links_post_bulk.go, where the endpoint takes a bare JSON array as its body), a literal wildcard (body[\"pre_attachment[*]\"]), and unsubstituted doc placeholders (body[\"grade_data[<student_id>][posted_grade]\"], body[\"<placement_name>[<placement_configuration_key>]\"]). Form encoding transmits these as literally as JSON did; they need generator-side fixes.",
  "validated_outcome": "Root cause reproduced and fixed against a live Canvas instance. Before: PUT with {\"wiki_page[body]\": \"...\"} as JSON returned HTTP 200 with the page body unchanged; POST /pages returned 400 \"Title can't be blank\" despite sending a title. After: the same CLI invocations change the page and create it correctly. Controls confirmed the diagnosis -- nested JSON ({\"wiki_page\":{\"body\":...}}) and form-encoded (wiki_page[body]=...) both wrote successfully where flat-bracketed JSON did not. Unit tests cover the bracket shapes the tree actually emits (parent[child], list[], list[][field], deep nesting, bool/number rendering, nil omission, reserved-character escaping) and assert non-bracketed bodies still go out as application/json. One encoder bug was caught by those tests before shipping: keys already containing an empty bracket pair must repeat as-is rather than gain a second []. Full Go suite passes; --dry-run now prints the form body and its Content-Type instead of printing nothing."
}
